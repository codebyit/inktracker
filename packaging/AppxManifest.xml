<?xml version="1.0" encoding="utf-8"?>
<!--
  InkTrack — Microsoft Store MSIX manifest (Desktop Bridge / Win32 full-trust).

  This is a TEMPLATE. The placeholder tokens below are filled in by
  packaging/make-msix.ps1 (or you can copy this file and replace them by hand):

    {{IDENTITY_NAME}}           Package identity name reserved in Partner Center,
                                e.g. 12345CompanyName.InkTrack
    {{PUBLISHER}}               Identity publisher — MUST exactly match your
                                Partner Center publisher, e.g.
                                CN=ABCD1234-...-WXYZ (from Partner Center) or
                                CN=Your Real Name (for a self-signed sideload cert)
    {{PUBLISHER_DISPLAY_NAME}}  Human-readable publisher shown in the Store
    {{VERSION}}                 4-part version, e.g. 0.10.0.0

  Package layout produced by make-msix.ps1 (relative to this manifest):
    AppxManifest.xml
    Assets\...                (visual assets)
    InkTrack\InkTrack.exe     (the PyInstaller onedir output + _internal\)
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap10="http://schemas.microsoft.com/appx/manifest/uap/windows10/10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap uap10 rescap">

  <Identity
    Name="{{IDENTITY_NAME}}"
    Publisher="{{PUBLISHER}}"
    Version="{{VERSION}}"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>InkTrack</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY_NAME}}</PublisherDisplayName>
    <Description>InkTrack — UV print cost tracker: projects, COGS, and ink inventory.</Description>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>

  <Applications>
    <!--
      uap10:RuntimeBehavior="packagedClassicApp" + TrustLevel="mediumIL" is the
      modern full-trust Win32 entry (Windows 10 2004 / 10.0.19041+). For older
      targets, remove those two attributes and use instead:
          EntryPoint="Windows.FullTrustApplication"
    -->
    <Application Id="InkTrack"
      Executable="InkTrack\InkTrack.exe"
      uap10:RuntimeBehavior="packagedClassicApp"
      uap10:TrustLevel="mediumIL">
      <uap:VisualElements
        DisplayName="InkTrack"
        Description="UV print cost tracker"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile
          Wide310x150Logo="Assets\Wide310x150Logo.png"
          Square71x71Logo="Assets\Square71x71Logo.png"
          Square310x310Logo="Assets\Square310x310Logo.png" />
      </uap:VisualElements>
    </Application>
  </Applications>
</Package>
